sudo docker inspect -f '{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}' onos
sudo mn --controller=remote,ip=172.17.0.2,port=6653 --topo tree,2

sudo docker rm -f onos
sudo docker run -t -d \
  -p 8181:8181 \
  -p 8101:8101 \
  -p 6653:6653 \
  --name onos \
  onosproject/onos:2.7.0




sudo docker run -t -d -p 8181:8181 -p 8101:8101 -p 6653:6653 --name onos onosproject/onos:2.7.0
